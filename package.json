{
  "scripts": {
    "build:bundle": "webpack --config webpack.config.js",
    "build:styles": "esbuild index.css --bundle --minify --outfile=dist/content.css && esbuild fontawesome.css --bundle --minify --outfile=dist/fontawesome.css --loader:.woff2=file --asset-names=webfonts/[name] && echo. && echo Build finished."
  },
  "devDependencies": {
    "webpack-cli": "^6.0.1"
  }
}
