{
  "scripts": {
    "bundle": "webpack --config webpack.config.js",
    "build:style": "esbuild ui/index.css --bundle --minify --outfile=dist/content.css && esbuild fontawesome.css --bundle --minify --outfile=dist/fontawesome.css --loader:.woff2=file --asset-names=webfonts/[name] && echo. && echo Build finished."
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "css-loader": "^7.1.4",
    "style-loader": "^4.0.0",
    "webpack": "^5.105.4",
    "webpack-cli": "^6.0.1"
  }
}
